# DEPRECATED — replaced by register_schedule_tasks.ps1
# This file registered an always-on startup task (not efficient).
# Use register_schedule_tasks.ps1 instead — starts/stops on schedule.
Write-Host "This script is deprecated. Run register_schedule_tasks.ps1 instead." -ForegroundColor Red
